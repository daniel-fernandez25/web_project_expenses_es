console.log("Hello from fnm-managed Node!");
console.log("Node version:", process.version);
//This is a placeholder script to debug. You could technically replace it with your own code, but the file must exist so the debugger has something to run.
